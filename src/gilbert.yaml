version: 1.0
vars:
  buildDir: './build'

tasks:
  serve:
  - action: shell
    params:
      rawOutput: true
      command: hugo server -D
  build:
  - action: shell
    params:
      rawOutput: true
      command: hugo -d ../
