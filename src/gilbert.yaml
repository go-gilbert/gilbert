version: 1.0
vars:
  buildDir: './build'

tasks:
  serve:
  - plugin: shell
    params:
      rawOutput: true
      command: hugo server -D
  build:
  - plugin: shell
    params:
      rawOutput: true
      command: hugo -d ../
