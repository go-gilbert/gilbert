{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "dbg: gilbert run build",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/gilbert",
      "cwd": "${workspaceFolder}",
      "args": [
        "run",
        "build",
        "--cwd",
        "docs/testdata",
        "--log-level=debug",
        "--items=1,2,3"
      ]
    }
  ]
}
